export = ignoreStream;
/**
 * Safely ignores a Node.js readable stream.
 * @param {import("stream").Readable} stream Node.js readable stream.
 */
declare function ignoreStream(stream: import("stream").Readable): void;
